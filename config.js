/* Supabase connection settings.
   These two values are safe to be public in client-side code — the anon key
   only grants what your Row Level Security policies allow (see README.md).
   Fill these in after you create your Supabase project. */
window.SUPABASE_URL = "REPLACE_WITH_YOUR_SUPABASE_PROJECT_URL";
window.SUPABASE_ANON_KEY = "REPLACE_WITH_YOUR_SUPABASE_ANON_PUBLIC_KEY";
